# GitHub 同步提交规则

## 适用范围

本规则适用于工作流简单、以 `main` 作为唯一协作分支的项目。可将本文件复制到其他项目根目录后直接复用。

## 基本原则

- 只有用户明确提出“同步到 GitHub”、“提交到 GitHub”或“push 到 GitHub”时，才进入同步流程。
- 未收到明确同步指令时，所有改动仅保留在本地工作区，不自动提交或推送。
- 所有同步均直接提交并推送到远端 `main` 分支。
- 不创建功能分支，不发起 Pull Request（PR），不执行分支合并流程。
- 同步前必须取得用户对本次具体变更和提交信息的明确确认。

## 执行流程

1. 确认当前检出分支为 `main`，并识别远端仓库及其 `main` 分支。
2. 比较本地工作区、本地 `main` 和远端 `origin/main` 的差异；同时检查远端是否存在需先处理的新提交。
3. 列出本次计划同步的更新项目，至少包括：
   - 新增、修改、删除的文件；
   - 每项变更的简要说明；
   - 任何可能影响同步的冲突、未跟踪文件或远端差异。
4. 根据实际差异拟定提交信息。提交信息必须概括本次变更的内容，不使用与差异无关的固定或泛化描述。
5. 向用户展示“更新项目清单”和“拟提交信息”，并明确询问：**是否确认同步/提交？**
6. 只有在用户作出明确确认后，才将已确认的改动提交到本地 `main`，并推送至 `origin/main`。
7. 完成后反馈提交哈希、提交信息及推送结果。

## 异常处理

- 若远端 `main` 已有本地未包含的提交，先说明差异与处理建议；未获用户确认，不继续提交或推送。
- 若存在冲突、无法安全判断是否应纳入的文件，先列出问题并等待用户决定。
- 用户确认的范围以展示给用户的更新项目清单为准；范围发生变化时，重新展示并再次请求确认。
